# R8 — Human Recall Judging Session (Goga, assessor of record)

The first exercise of the final-authority tier (ADR 0121 R8; JUDGING.md
protocol). Four recall queries, each drawn from demand that ACTUALLY
occurred in real sessions this week — not drafted hypotheticals. Results
below are live runs against the current store (2026-07-18, per-home;
fused `--home all` is quarantined behind TASK-0006 and will be re-judged
after the fix).

**Grade scale:** 3 = the answer (what you needed) · 2 = genuinely useful
alongside the answer · 1 = marginal, cost tokens without paying · 0 =
should not have surfaced. Also judge what is MISSING: a memory that should
have been returned but wasn't is a finding of equal weight.

**How to record:** tell granite grades + one-line rationales in chat (he
records them with tier `human:goga 2026-07-18`), or edit this file
directly — both are docs-native.

---

## Q1 — "conflict markers rebase resolution" (home: global)

Provenance: granite, dogfood session 01 — asked for real before a manual
conflict resolution.

| rank | score | id | title | grade | rationale |
|---|---|---|---|---|---|
| 1 | 1.525 | MEMO-0011 | Diff3 conflict markers: check for \|\|\|\|\|\|\| too | _3_ | seems useful for the question |
| 2 | 0.807 | MEMO-0008 | Granite merge law: rebase-first, ff-only, first-person capture | _2_ | seems somewhat useful but less relevant towards conflict resolution |
| 3 | 0.214 | MEMO-0010 | Granite re-prime order: wakeup first, harness anchors second | _1_ | re priming on wakeup doesn't seem relevant to conflict resolution, costs tokens without value |

Missing (if any): __

## Q2 — "how do we cut a release" (home: project)

Provenance: granite, dogfood session 01 — asked for real; initially
soft-missed (before MEMO-0002 existed), the miss triggered writing the
memory. Judging today's post-fix state.

| rank | score | id | title | grade | rationale |
|---|---|---|---|---|---|
| 1 | 0.922 | MEMO-0002 | Release lane: changelog retitle, lockstep bumps, fetch-first push, CI auto-tag | _3_ | seems quite relevant for releasing the package, i feel like this should have had much higher score than the 2nd place here, why are they so close in scores with eachother its weird |
| 2 | 0.783 | MEMO-0001 | Telemetry that observes ambient state can also arm it; sibling sinks | _0_ | seems quite irrelevant for the question |

Missing (if any): __

## Q3 — "what is my merge law before landing a branch" (home: global)

Provenance: granite's covenant re-primes — the question every landing
implicitly asks; recorded in re-prime recalls.

| rank | score | id | title | grade | rationale |
|---|---|---|---|---|---|
| 1 | 1.291 | MEMO-0008 | Granite merge law: rebase-first, ff-only, first-person capture | _3_ | seems relevant but memory contains lots of other information and context that might not be needed for this ask |
| 2 | 1.044 | MEMO-0012 | Check what the harness already provides before claiming reachability wins | _0_ | seems quite irrelevant to the question asked, wastes tokens and with such a high score makes me question our scoring qrels logic entirely |
| 3 | 0.895 | MEMO-0009 | Phase Two day-2 checkpoint: measured release loop closed | _0_ | unsure why does this memory even exist, why does it have such a high score and what purpose does it serve |

Missing (if any): __

## Q4 — "how to test wakeup grounding against real repo state" (home: project)

Provenance: the W2 engineer's memfs lesson (MEMO-0005) was REUSED by the
desk-hardening builder the same day ("MEMO-0005's memfs lesson applied to
my tests" — its final report). This query is the reuse moment, replayed.

| rank | score | id | title | grade | rationale |
|---|---|---|---|---|---|
| 1 | 1.122 | MEMO-0005 | Two-filesystem test compositions: absent fixture mirrors change semantics | _2_ | seems useful but a bit unclear about the question, what was the agent trying to solve or do, maybe we dont have no actual memories relevant to the question? |
| 2 | 0.724 | MEMO-0007 | Bounded folds have two omission channels: counts vs named-degradation text | _1_ | i am not sure how this is relevant |
| 3 | 0.693 | MEMO-0001 | Telemetry that observes ambient state can also arm it; sibling sinks | _1_ | i am not sure how this is relevant |

Missing (if any): _MEMO-0006_ seems more relevant to this question in my opinion that 7 and 1

---

On completion: granite writes the four queries + graded qrels to
`recall-queries-v2.jsonl` / qrels with assessor `human:goga 2026-07-18`,
baseline v2 unblocks, and the ranking freeze (0121 R5) gains its first
legitimate evidence gate. The frozen-lane queue (T1 contextual chunking,
T2 projection weighting) becomes actionable.
