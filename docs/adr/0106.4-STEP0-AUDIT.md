# Step-0 Classification Audit — ADR 0106.4 (`description` → `content`)

**Task**: TASK-0690
**Date**: 2026-06-17
**Rule**: Blind search/replace is forbidden. `description` is overloaded. Every hit
below is classified **(a) RENAME** (entity body) or **(b) LEAVE** (not the body).
Baseline before edits: `pnpm -r typecheck` clean; server suite **885 passed / 2 skipped**;
viewer **96 passed**.

Build artifacts (`packages/*/dist/**/*.d.ts`) are excluded — regenerated from source.

---

## (a) RENAME → `content` (entity body)

### Schema (`packages/shared/src/substrates/`)
| File:Line | Hit | Action |
|---|---|---|
| `base.ts:42` | `description: z.string().optional()` | → `content: z.string().optional()` |
| `memory.ts:60` | `description: z.string().min(1)` (override) | → `content: z.string().min(1)` |
| `memory.ts:15,56,57` | doc-comment prose ("markdown description") | optional wording tidy; keep "body IS the content" note |

No other substrate (task, epic, folder, artifact, milestone, cron) overrides the
body field — they all inherit `BaseEntitySchema`. Confirmed via grep: only `base.ts`
and `memory.ts` carry a `description:` schema line in `shared/src`.

### Storage seams (`packages/server/src/storage/`)
| File:Line | Hit | Action |
|---|---|---|
| `local/filesystem-storage.ts:35` | `const { description, ...frontmatter } = entity;` | destructure `content` (gray-matter body is positional) |
| `local/filesystem-storage.ts:36` | `matter.stringify(description || '', frontmatter)` | use `content` |
| `local/filesystem-storage.ts:40` | `const { data, content: description } = matter(content)` | **name clash**: `matter()` returns `content` for the body AND the param is `content`. Rename param → `raw`; alias matter body → `body`; assign `content: body.trim()`. |
| `local/filesystem-storage.ts:41` | `{ ...data, description: description.trim() }` | `{ ...data, content: ... }` |
| `d1/d1-storage.ts:79` | `entity.description = row.body.trim()` | `entity.content = row.body.trim()` |
| `d1/d1-storage.ts:173` | `const body = toNull(t.description)` | `toNull(t.content)` |
| `d1/d1-storage.ts:207` | `const body = toNull(t.description)` | `toNull(t.content)` |
| `d1/d1-storage.ts:42` | comment `// markdown body / description` | tidy comment → "content" |

**D1 SQL column `body` STAYS.** Lines 117,127,180,214,229,347,365,371,379 reference SQL
`body` / `tasks_fts` `body` — **LEAVE** (population b, see below). Only the
`entity.<field>` JS side renames.

### Search (`packages/memory/src/search/`)
| File:Line | Hit | Action |
|---|---|---|
| `orama-schema.ts:9` | `description: string` (OramaDoc type) | → `content: string` |
| `orama-schema.ts:29` | `description: 'string'` (schema) | → `content: 'string'` |
| `orama-schema.ts:64` | `TEXT_PROPERTIES = ['title','description',...]` | → `'content'` |
| `orama-schema.ts:70` | `UNSORTABLE_PROPERTIES = [...,'description',...]` | → `'content'` |
| `orama-schema.ts:49` | `INDEX_VERSION = 4` | **bump → 5** (schema field renamed forces index rebuild) |
| `orama-search-service.ts:92` | `task.description \|\| ''` | `task.content` |
| `orama-search-service.ts:109` | `description: task.description \|\| ''` (taskToDoc) | `content: task.content \|\| ''` |
| `orama-search-service.ts:125` | `description: resource.content` (resourceToDoc) | key → `content: resource.content` (OramaDoc field renamed) |
| `orama-search-service.ts:376` | `task.description \|\| ''` | `task.content` |
| `orama-search-service.ts:640` | `boost: { title: 2, description: 1 }` | `content: 1` (OramaDoc field renamed) |
| `snippets.ts:21` | `{ name: 'description', value: task.description \|\| '' }` | `name: 'content', value: task.content` |
| `scoring.ts:50,175` | prose comments only | optional tidy |
| `types.ts:66` | comment listing field names | optional tidy |

Note `snippets.ts:35` `{ name: 'content', value: resource.content }` is the **resource**
snippet — already `content`; no change. After the task-body rename the entity-body
snippet field name also becomes `content`, unifying task + resource snippet vocab.

### Core (`packages/server/src/core/`)
| File:Line | Hit | Action |
|---|---|---|
| `types.ts:124` | `CreateParams.description?: string` | → `content?` |
| `types.ts:198` | `SearchResultItem.description?: string` | → fold into `content?` (line 199 already has `content?`) — keep single `content?` |
| `create.ts:35` | destructure `description` from params | `content` |
| `create.ts:44` | `createEntity({ ...description ... })` | `content` |
| `create.ts:23,33` | doc-comment prose | tidy |
| `edit.ts:24` | `applyOperation(task.description ?? '', ...)` | `task.content` |
| `edit.ts:25` | `service.save({ ...task, description: newBody, ... })` | `content: newBody` |
| `search.ts:41` | `item.description = task.description` | `item.content = task.content` |

`core/types.ts:111` `GetItem.content` and `:199` `SearchResultItem.content` are **already
`content`** — the search result will collapse to a single `content?` field (remove the
now-redundant `description?` at :198). Update `core/search.ts:41` + `:31` accordingly.

### Memory plugin (`packages/server/src/memory/`)
| File:Line | Hit | Action |
|---|---|---|
| `capture.ts:52` | `entity.description ?? ''` | `entity.content` |
| `backlog-memory-store.ts:72` | `description: entry.content` (writes Memory entity field) | `content: entry.content` |
| `backlog-memory-store.ts:199` | `content: m.description` (toMemoryEntry) | `content: m.content` — **the seam translation collapses** |

`MemoryEntry.content` (the plugin type) is **already `content`** — unchanged. This is
the whole point: `toMemoryEntry` stops translating.

### Context stages (`packages/server/src/context/`)
| File:Line | Hit | Action |
|---|---|---|
| `types.ts:40` | `ContextEntity.description?: string` | → `content?` |
| `stages/focal-resolution.ts:38` | `if (task.description) entity.description = task.description` | `task.content`/`entity.content` |
| `stages/semantic-enrichment.ts:51,52` | `focalTask.description` reads | `focalTask.content` |
| `stages/semantic-enrichment.ts:12,14,47` | prose comments | tidy |
| `token-budget.ts:61` | `if (entity.description) cost += estimateTokens(entity.description)` | `entity.content` |
| `token-budget.ts:60` | comment | tidy |
| `tools/backlog-context.ts:157` | `if (entity.description) out.description = entity.description` | `entity.content`/`out.content` |

### Tools / CLI (public MCP+CLI field — ADR 0106.4 decision: rename to `content`)
| File:Line | Hit | Action |
|---|---|---|
| `tools/backlog-create.ts:16` | `description: z.string()...describe('Task description in markdown')` | field → `content`; describe text → "Task body in markdown" |
| `tools/backlog-create.ts:17` | `source_path` describe mentions "description" | reword → "content" |
| `tools/backlog-create.ts:27,28` | refine: `data.description && data.source_path` + message | `data.content`; message wording |
| `tools/backlog-create.ts:33,35,40,42` | handler local `description` var + `params.description` | rename local → `content`; pass `{ ...params, content }` |
| `cli/commands/create.ts:10` | `.option('--description <text>', 'Description in markdown')` | → `--content <text>`, 'Content in markdown' |
| `cli/commands/create.ts:11` | `--source` help "Read description from file" | reword |
| `cli/commands/create.ts:17,20` | `opts.description` → `createItem({ description })` | `opts.content` → `content` |
| `README.md:170,174,212` | example `description="..."` + comments | → `content="..."`; reword comments |

### Viewer (`packages/viewer/`)
| File:Line | Hit | Action |
|---|---|---|
| `utils/api.ts:17` | `Task.description?: string` | → `content?` (server now returns `content`) |
| `components/task-detail.ts:66` | `task.value?.description ?? ''` | `task.value?.content` |
| `components/document-view.ts:35` | `HEADER_KEYS` set includes `'description'` | → `'content'` (exclude body from MetadataCard) |
| `components/spotlight-search.ts:66` | `{ name: 'description', value: task.description \|\| '' }` | `name: 'content', value: task.content` |

### Tests (`packages/server/src/__tests__/`, ~151 hits)
All entity-body `makeEntity({ description })`, `expect.objectContaining({ description })`,
`task.description` reads, `delete task.description`, frontmatter builders, and assertions
on `result.description` → **RENAME to `content`**. Because `MemorySchema`/substrates are
`.strict()` after the schema rename, leaving any entity-literal `description:` would throw
at parse time. Sweep all 18 test files. Prose like "should preserve description with
markdown" → reword to "content". The `query-intent.test.ts:23` `makeEntity` typed param
`description?:` → `content?:`.

**Exception inside tests — LEAVE**: any `error_description` (OAuth) and any
`.describe(...)`/tool-schema `description:` assertions (population b). Audit each test
hit against the (b) list before renaming.

---

## (b) LEAVE UNTOUCHED (not the entity body)

### MCP tool-schema `description:` keys and `.describe()` calls
These describe the **tool or a parameter**, not the entity body:
- `tools/backlog-delete.ts:12`, `backlog-update.ts:13`, `backlog-list.ts:11`,
  `backlog-context.ts:40`, `backlog-create.ts:13`, `backlog-search.ts:11`,
  `backlog-get.ts:27`, `backlog-write-resource.ts:12`, and the multi-line tool
  `description:` blocks in `backlog-wakeup.ts:40`, `backlog-consolidation.ts:26`,
  `backlog-forget.ts:27`, `backlog-recall.ts:37`, `backlog-remember.ts:32`,
  `backlog-contradictions.ts:23` — **tool descriptions, LEAVE**.
- `backlog-list.ts:17` `.describe('Search across all task fields (title, description, …)')`
  — parameter help text; the word is descriptive prose. **LEAVE** (could optionally update
  the wording to say "content" for accuracy, but it is not a renamed identifier). Decision:
  update the user-facing wording to "content" for honesty, but it is NOT a code rename.
- `backlog-search.ts:19` `.describe('Include full description/content in results…')` — help
  text. Optional wording update only.
- `tools/backlog-consolidation.ts:9`, `backlog-contradictions.ts:8` — comments referencing
  "the tool description". **LEAVE**.

### OAuth — `error_description` / client descriptions
- `auth/oauth-routes.ts:240`, `auth/oauth-token.ts:27,75,78,83,91` — OAuth2 spec
  `error_description` field. **LEAVE**.

### Resource registration metadata
- `resources/manager.ts:170` `{ description: 'Any file in the backlog data directory' }`
  — MCP **resource registration metadata** (describes the resource template to clients),
  not an entity body. **LEAVE**.

### Commander `.description()` (command help)
- `cli/index.ts:24,32,37,64,94,98` and every `cli/commands/*.ts` `.description('…')` call
  (search, context, get, remember, recall, contradictions, edit ×4, consolidation, forget,
  update, create, list, wakeup, delete) — these are Commander **command descriptions**, a
  method call unrelated to the entity body. **LEAVE.**
  - Exception: `create.ts` `.option('--description …')` IS the body input flag → RENAME
    (listed in population a). The `.description('Create a new backlog item')` on the same
    command is command help → LEAVE.

### D1 SQL column + FTS
- `d1-storage.ts` SQL string literals: column `body`, `tasks_fts(... body)`,
  `SELECT body`, `body LIKE ?` (lines ~117,127,180,214,229,347,365,371,379). Column name
  is `body` and **does not change** (ADR: no SQL migration). Only the `entity.<field>` JS
  property renames.

### On-disk data
- `<dataDir>/tasks/*.md` — positional gray-matter body, not a keyed field. **No migration.**

### package.json / ADR history
- Any `package.json` `"description"` — **LEAVE**.
- `docs/adr/*.md` (historical ADRs, including `0053-remove-description-from-backlog-update`)
  — historical record. **LEAVE.** Only `README.md` user-facing examples update.

---

## Execution order (commit per chunk)
1. **shared schema** (`base.ts`, `memory.ts`) — the type pivot.
2. **storage seams** (`filesystem-storage.ts`, `d1-storage.ts`).
3. **search** (`orama-schema.ts` incl. INDEX_VERSION bump, `orama-search-service.ts`,
   `snippets.ts`).
4. **core + memory + context + tools(context)** (`core/*`, `memory/*`, `context/*`,
   `backlog-context.ts`).
5. **public surface**: `backlog-create.ts` tool, `cli/commands/create.ts`, README.
6. **viewer** (`api.ts`, `task-detail.ts`, `document-view.ts`, `spotlight-search.ts`).
7. **tests** sweep (all 18 files) — run between chunks; full green at the end.

Gate: `pnpm -r typecheck` clean; server **885/2 skipped**; viewer green; manual
create/get/edit/remember/recall/search round-trip.
</content>
