---
id: MEMO-0010
title: >-
  Focus is never-yield by construction — lines added to it are free of trim
  logic but cost permanent bytes
parent_id: FLDR-0001
created_at: '2026-07-18T16:29:27.566Z'
updated_at: '2026-07-18T16:29:27.566Z'
type: memory
layer: procedural
source: 'builder:legal-actions'
tags:
  - wakeup
  - disclosure
  - budget
kind: timeless
---
Building the legal-next-actions line (compiled-process 5% slice) I learned the wakeup focus object is never-yield by CONSTRUCTION: trimOnce in wakeup-wire.ts simply never touches focus, so anything attached to it inherits the never-yield class with zero new trim code — the flip side is every byte added there is unconditional wire tax, so it must stay one line and the wire-budget fixtures must be re-run to prove the ceiling holds (mine fit: focal pressure payload 2,987 of 3,072). Two seams made the feature nearly free: listWakeupDisclosures entries are the right channel for per-substrate declaration facts (I rode the substrate's existing workflow declaration through it — no new declaration kind), and matchesDeclaredStatus is the ONE status-comparison law, so freeform human states ('Live (goga, ...)') matched workflow transition from-states with no extra handling.
